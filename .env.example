# Configuration Qwota App
# Copier ce fichier en .env et remplir les valeurs

# Sécurité
JWT_SECRET_KEY=votre_cle_secrete_tres_longue_et_aleatoire_ici_min_32_caracteres
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=10080  # 7 jours

# Base de données
DATABASE_PATH=data/qwota.db

# Mots de passe par défaut (À CHANGER EN PRODUCTION!)
SUPPORT_DEFAULT_PASSWORD=Support@2025_CHANGE_ME
DIRECTION_DEFAULT_PASSWORD=Direction@2025_CHANGE_ME

# CORS - Domaines autorisés (séparés par des virgules)
ALLOWED_ORIGINS=http://localhost:8080,http://localhost:3000

# Application
ENV=development  # development ou production
DEBUG=true
HOST=0.0.0.0
PORT=8080

# Cloud storage
BASE_CLOUD_PATH=data

# Limites
MAX_FILE_SIZE_MB=5
MAX_UPLOAD_FILES=10
RATE_LIMIT_PER_MINUTE=60
LOGIN_RATE_LIMIT_PER_MINUTE=5

# Session
COOKIE_MAX_AGE_DAYS=7
SESSION_COOKIE_NAME=qwota_session
